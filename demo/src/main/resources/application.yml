spring:
  application:
    name: demo
  datasource:
    url: jdbc:mysql://localhost:3306/mybatis_plus_test?useSSL=true&requireSSL=true&verifyServerCertificate=false   # or configure CA properly and set true&serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl